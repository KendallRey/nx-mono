import { CardActions, CardActionsProps } from "@mui/material";
import React from "react";

export const MuiCardActions: React.FC<CardActionsProps> = (props) => {
  return <CardActions {...props} />;
};
